type

Student
{
    str name;
    int marks;
}

endtype

int main()
{
decl
    int val,temp;
    Student var;
enddecl
begin
    val = initialize();
	var = alloc();
	var.name="John";
	var.marks=89;
	temp=exposcall ("Write",-2,var.name);
	temp=exposcall ("Write",-2,var.marks);

    return 0;
end
}
